{
  "name": "@runtimed/node",
  "version": "0.0.1",
  "description": "Node.js bindings for the runtimed daemon client (napi-rs).",
  "type": "module",
  "license": "BSD-3-Clause",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nteract/desktop.git",
    "directory": "packages/runtimed-node"
  },
  "main": "./src/index.cjs",
  "types": "./src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "require": "./src/index.cjs",
      "default": "./src/index.cjs"
    }
  },
  "files": [
    "src/index.cjs",
    "src/index.d.ts",
    "src/binding.cjs",
    "src/binding.d.ts",
    "src/runtimed-node.*.node"
  ],
  "napi": {
    "binaryName": "runtimed-node",
    "targets": [
      "x86_64-unknown-linux-gnu"
    ]
  },
  "scripts": {
    "build": "napi build --platform --release --js binding.cjs --dts binding.d.ts --manifest-path ../../crates/runtimed-node/Cargo.toml --output-dir src",
    "build:debug": "napi build --platform --js binding.cjs --dts binding.d.ts --manifest-path ../../crates/runtimed-node/Cargo.toml --output-dir src",
    "prepack": "node -e \"const fs = require('node:fs'); const files = fs.readdirSync('src'); if (!files.includes('binding.cjs') || !files.some((file) => /^runtimed-node\\\\..+\\\\.node$/.test(file))) { console.error('Run pnpm --dir packages/runtimed-node build before packing @runtimed/node.'); process.exit(1); }\"",
    "pack:dry-run": "pnpm pack --dry-run",
    "smoke": "node -e \"const m = require('./src/index.cjs'); console.log('defaultSocketPath:', m.defaultSocketPath());\""
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.6.2"
  }
}
