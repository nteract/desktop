{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "upgrade",
  "description": "Minimal capabilities for the upgrade window",
  "windows": ["upgrade"],
  "permissions": [
    "core:default",
    "core:event:default",
    "updater:default",
    "process:allow-restart"
  ]
}
