[project]
name = "nteract-workspace"
version = "0.0.0"
description = "Development workspace for nteract Python packages"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["runtimed", "nteract"]

[tool.uv.sources]
runtimed = { workspace = true }
